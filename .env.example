# Production Environment Variables Template
# =============================================================================
# This file is a TEMPLATE for production/Vercel deployment
# DO NOT commit the actual .env or .env.production files with real values
#
# For Local Development:
# - Use .env.local (already configured with Docker, safe to use as-is)
# - Run: docker-compose up -d
#
# For Production (Vercel):
# - Copy this file: cp .env.example .env.production
# - Fill in real values from MongoDB Atlas and your Redis provider
# - Set in Vercel dashboard: Settings → Environment Variables
# =============================================================================

# MongoDB Atlas Configuration
# Get from: https://cloud.mongodb.com/
# 1. Create cluster → Connect → Connect your application
# 2. Copy connection string and replace <password>
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=psz-sketch

# Redis Configuration
# Use any Redis provider (Vercel KV, Railway, Redis Labs, etc.)
# Note: Use rediss:// (with double 's') for TLS in production
REDIS_URL=rediss://default:your-password@your-redis-endpoint:6379

# JWT Secret for Token Signing
# Generate securely: openssl rand -base64 32
# IMPORTANT: Use a different secret for production!
JWT_SECRET=your-secure-secret-key-change-in-production
