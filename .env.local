# Local Development Environment
# =============================================================================
# This file is SAFE TO COMMIT to source control.
# It contains only local Docker credentials that work exclusively with the
# Docker containers defined in docker-compose.yml
#
# These credentials are:
# - Hardcoded in docker-compose.yml (already public)
# - Only accessible on localhost
# - Not used in production
#
# Quick Start:
# 1. Run: docker-compose up -d
# 2. Run: npm run dev
# 3. Open: http://localhost:4321
# =============================================================================

# MongoDB Configuration (Docker)
MONGODB_URI=mongodb://psz_user:psz_password@localhost:27017/psz-sketch?authSource=admin
MONGODB_DB_NAME=psz-sketch

# Redis Configuration (Docker)
REDIS_URL=redis://localhost:6379

# JWT Secret for Development
# Generate a new one for production: openssl rand -base64 32
JWT_SECRET=dev-secret-change-in-production-12345678

# Optional: If you want to use a cloud Redis provider instead of local Redis
# Uncomment and set your Redis URL:
# REDIS_URL=rediss://default:your-password@your-redis-endpoint:6379

# Optional: If you want to use MongoDB Atlas instead of local MongoDB
# Uncomment and set your MongoDB Atlas connection string:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
